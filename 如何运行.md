# 煤矿审查系统 v9 运行手册

本机当前状态：

- Docker Desktop / WSL 已安装。
- Milvus Standalone 已通过 Docker 跑通。
- MongoDB / Redis 已加入 `docker-compose.data.yml`；MongoDB 用于长期业务记录镜像，Redis 目前仅作为后续短期状态层预留。
- 旧 Chroma `kb_2` 已迁移到 Milvus，当前 Milvus 中 `kb_2` 有 493 条 chunk。
- `langchain0.3` 环境已安装 `pymilvus`、MinerU、v9 worker 所需依赖；若新拉环境，需要再安装 `pymongo`。

所以以后日常使用时，通常不用再安装依赖，也不用再迁移 Chroma。按下面“日常启动”即可。

---

## 一、日常启动

### 可选：启动业务数据层 MongoDB / Redis

如果要验证“历史待审文档长期保存到 MongoDB”或后续 Redis 短期状态层，先启动：

```powershell
cd D:/work/project/coal-regulation-ai
docker compose -f docker-compose.data.yml up -d
```

确认状态：

```powershell
docker compose -f docker-compose.data.yml ps
```

后端当前策略：

- MongoDB 可用：审查任务、问题、人工反馈会镜像到 MongoDB，历史列表优先读 MongoDB。
- MongoDB 不可用：自动退回现有 SQLite 队列，不影响审查。
- Redis 已有本地容器配置，但当前 v9 审查还没有把实时状态切到 Redis。

后端存储健康检查：

```text
http://localhost:8000/api/v9/storage-health
```

### 场景 A：只验证 RAG / 知识库检索

需要 3 个服务：

| 终端 | 服务 | 是否必须 |
| --- | --- | --- |
| 1 | Milvus Standalone | 必须 |
| 2 | 后端 FastAPI | 必须 |
| 3 | 前端 React | 必须 |

#### 1. 启动或确认 Milvus

```powershell
cd D:/work/project/coal-regulation-ai
docker compose -f docker-compose.milvus.yml up -d
```

确认 Milvus 还在：

```powershell
docker compose -f docker-compose.milvus.yml ps
```

正常应看到：

```text
coal-regulation-milvus-etcd
coal-regulation-milvus-minio
coal-regulation-milvus-standalone
```

如果想确认 Python 能连上：

```powershell
D:/Anaconda/envs/langchain0.3/python.exe -c "from pymilvus import MilvusClient; c=MilvusClient(uri='http://localhost:19530'); print(c.list_collections())"
```

#### 2. 启动后端

```powershell
cd D:/work/project/coal-regulation-ai/backend
D:/Anaconda/envs/langchain0.3/python.exe main.py
```

看到下面内容表示后端可用：

```text
Uvicorn running on http://0.0.0.0:8000
```

接口文档：

```text
http://localhost:8000/api/docs
```

#### 3. 启动前端

```powershell
cd D:/work/project/coal-regulation-ai
npm.cmd run dev
```

浏览器打开：

```text
http://localhost:3000
```

---

### 场景 B：要上传新的 PDF / DOC / DOCX 到知识库

比场景 A 多开 1 个 MinerU API 终端。

| 终端 | 服务 | 是否必须 |
| --- | --- | --- |
| 1 | Milvus Standalone | 必须 |
| 2 | MinerU API | 上传 PDF/DOC/DOCX 时必须 |
| 3 | 后端 FastAPI | 必须 |
| 4 | 前端 React | 必须 |

#### 1. 启动 Milvus

```powershell
cd D:/work/project/coal-regulation-ai
docker compose -f docker-compose.milvus.yml up -d
```

#### 2. 启动 MinerU API

```powershell
D:/Anaconda/envs/langchain0.3/Scripts/mineru-api.exe --host 127.0.0.1 --port 51071
```

保持这个终端开着。前端上传 PDF、DOC、DOCX 时，后端会自动调用这个 API，把文档转成 MinerU JSON，再切分入 Milvus。

说明：

- 上传 `.txt` 不需要 MinerU。
- 上传 `.pdf`、`.docx`、`.doc` 需要 MinerU。
- `.doc` 会先用 Word COM 转成 `.docx`，所以本机需要 Microsoft Word 和 `pywin32`。

#### 3. 启动后端

```powershell
cd D:/work/project/coal-regulation-ai/backend
D:/Anaconda/envs/langchain0.3/python.exe main.py
```

#### 4. 启动前端

```powershell
cd D:/work/project/coal-regulation-ai
npm.cmd run dev
```

---

### 场景 C：要上传新的待审 Word，并使用多智能体审查

多智能体审查上传的是“待审文档”，不是“知识库文档”。当前前端审查接口只支持 `.docx` / `.doc`。

新上传待审文档时也要开 MinerU API。MinerU 负责把待审 Word 解析成结构化 JSON 和待审 chunks；`v9_worker` 同时用 `docx_adapter` 保留 Word 段落索引 `source_blocks`，这样后续前端高亮、接受修改、回写 Word 才能继续工作。

当前待审 Word 不会写入 Milvus。法规依据仍来自 worker 加载的本地 v9 知识库 JSON / BGE 索引，不是 Web RAG 的 Milvus collection。

处理链路是：

```text
前端上传待审 Word
-> 后端预处理为 .docx / PDF 预览
-> v9_worker 调 MinerU API 生成待审 JSON/chunks
-> v9_worker 用 docx_adapter 保留 Word source_blocks
-> 本地 BGE/v9 知识库审查
-> 问题回写前端，可接受/驳回/改写并下载 Word
```

| 终端 | 服务 | 是否必须 |
| --- | --- | --- |
| 1 | MinerU API | 新上传待审 Word 时必须 |
| 2 | v9 worker | 审查 Word 时必须 |
| 3 | 后端 FastAPI | 必须 |
| 4 | 前端 React | 必须 |
| 可选 | Milvus | 当前 Word 审查不需要；RAG/知识库功能仍需要 |

先启动 MinerU API：

```powershell
D:/Anaconda/envs/langchain0.3/Scripts/mineru-api.exe --host 127.0.0.1 --port 51071
```

保持这个终端开着。

推荐启动 worker 时关闭历史升级队列，避免它一启动就处理旧的 `low_confidence` / `numeric_conflict`：

```powershell
cd D:/work/project/coal-regulation-ai
$env:V9_DISABLE_ESCALATION_LOOP="1"
D:/Anaconda/envs/langchain0.3/python.exe v9_worker.py
```

`v9_worker` 的合规/错别字/数值核验阶段默认按 4 个 chunk 并发，和 `hybrid_rag_review_v9.py` 的批量测试路径一致。需要手动调整时，在启动 worker 前设置：

```powershell
$env:V9_WEB_CHUNK_CONCURRENCY="4"
```

如果 Qwen 接口限流或本机压力过高，先降到 `2`；如果接口额度和机器都稳定，再尝试 `6` 或 `8`。

法规召回与重排阶段也有两个可调参数，默认即可：

```powershell
$env:V9_RETRIEVAL_RECALL_CONCURRENCY="4"
$env:V9_RETRIEVAL_CHUNK_GROUP_SIZE="16"
```

含义：

- `V9_RETRIEVAL_RECALL_CONCURRENCY`：法规候选召回的并发线程数。
- `V9_RETRIEVAL_CHUNK_GROUP_SIZE`：多少个待审 chunk 合并成一组送 reranker 重排。

一般不要把 group size 调太大；reranker 仍然是单 GPU 模型，过大可能显存压力上升。建议先用默认值，如果 `检索重排` 仍明显慢，再试 `24` 或 `32`。

默认情况下，worker 会对新上传的待审 Word 调用 MinerU。只有临时排查问题、想回到旧的 `docx_adapter` 直切路径时，才设置：

```powershell
$env:V9_USE_MINERU_FOR_PENDING="0"
```

看到下面内容表示 worker 就绪：

```text
[worker] 引擎就绪，进入轮询循环
```

然后另开后端和前端：

```powershell
cd D:/work/project/coal-regulation-ai/backend
D:/Anaconda/envs/langchain0.3/python.exe main.py
```

```powershell
cd D:/work/project/coal-regulation-ai
npm.cmd run dev
```

---

## 二、第一次安装或重建时才需要做

下面这些不是日常每次运行都做。只有换机器、删环境、重装依赖、清空数据库时才需要。

### 1. 安装 Python 依赖

```powershell
cd D:/work/project/coal-regulation-ai
D:/Anaconda/envs/langchain0.3/python.exe -m pip install -r backend/requirements.txt
```

检查：

```powershell
D:/Anaconda/envs/langchain0.3/python.exe -c "import fastapi, pymilvus, FlagEmbedding, torch; print('OK', torch.cuda.is_available())"
```

### 2. 安装前端依赖

```powershell
cd D:/work/project/coal-regulation-ai
npm.cmd install
```

如果 PowerShell 执行策略拦截 `npm run dev`，用：

```powershell
npm.cmd run dev
```

### 3. 安装 MinerU 和模型

日常只需要启动 `mineru-api.exe`。下面命令只在首次安装 MinerU 时执行。

```powershell
$env:MINERU_MODEL_SOURCE="modelscope"
D:/Anaconda/envs/langchain0.3/python.exe -m pip install mineru --isolated -i https://mirrors.aliyun.com/pypi/simple/
D:/Anaconda/envs/langchain0.3/Scripts/mineru-models-download.exe -s modelscope -m pipeline
```

不要默认安装 `mineru[all]`，它会额外拉 `torch/lmdeploy/ray/gradio` 等重依赖，容易影响当前 BGE 环境。

### 4. 安装 Docker Desktop / WSL

本机已经装好。换机器时才需要。

安装完成后检查：

```powershell
docker --version
docker compose version
wsl --status
```

Docker Desktop 不登录也能本地运行容器；界面里可以点 `Skip`。

### 5. 首次启动 Milvus

```powershell
cd D:/work/project/coal-regulation-ai
docker compose -f docker-compose.milvus.yml up -d
```

Milvus 数据目录：

```text
backend/data/milvus_standalone/
```

不要随手删除这个目录，否则 Milvus 里的向量数据会丢失。

### 6. 旧 Chroma 数据迁移到 Milvus

本机已经迁移过 `kb_2`，正常不用再跑。

只有这些情况才需要重新迁移：

- 清空了 Milvus 数据；
- 换了一台机器；
- 旧 Chroma 又新增了数据；
- 想重建 Milvus collection；
- 向量维度或 schema 发生变化。

迁移全部旧 Chroma `kb_*`：

```powershell
cd D:/work/project/coal-regulation-ai
D:/Anaconda/envs/langchain0.3/python.exe tools/migrate_chroma_to_milvus.py --drop-collection
```

只迁移当前本机已有的 `kb_2`：

```powershell
D:/Anaconda/envs/langchain0.3/python.exe tools/migrate_chroma_to_milvus.py --kb-id 2 --drop-collection
```

说明：

- 脚本会优先复用 ChromaDB 里已有的 embedding，不重新调用外部 embedding API。
- 如果加 `--probe-query`，probe 检索仍需要生成 query embedding，需要保证后端配置的 embedding 接口能访问。

---

## 三、如何判断该开哪些服务

| 你要做什么 | Milvus | MinerU API | v9 worker | 后端 | 前端 |
| --- | --- | --- | --- | --- | --- |
| 打开页面看看 | 可不开 | 可不开 | 可不开 | 必须 | 必须 |
| RAG 检索已有知识库 | 必须 | 可不开 | 可不开 | 必须 | 必须 |
| 上传 TXT 到知识库 | 必须 | 可不开 | 可不开 | 必须 | 必须 |
| 上传 PDF/DOC/DOCX 到知识库 | 必须 | 必须 | 可不开 | 必须 | 必须 |
| 上传新的待审 Word 并多智能体审查 | 可不开 | 必须 | 必须 | 必须 | 必须 |
| 审查后接受/驳回/改写 Word | 可不开 | 只处理已解析任务可不开；新上传必须 | 必须 | 必须 | 必须 |
| 同时验证 RAG/知识库和审查 | 必须 | 上传 PDF/DOC/DOCX 到知识库或新待审 Word 时必须 | 审查时必须 | 必须 | 必须 |

---

## 四、前端使用流程

### RAG / 知识库

1. 启动 Milvus、后端、前端。
2. 如果要上传 PDF/DOC/DOCX，再启动 MinerU API。
3. 进入前端 RAG 或知识库页面。
4. 上传文档后，文档状态应从 `processing` 变成 `indexed`。
5. 文档详情中应能看到 chunks。
6. RAG 检索应能返回来源片段。

### 多智能体审查

1. 启动 MinerU API、v9 worker、后端、前端。
2. 进入左侧菜单「审查」页。
3. 选择矿井类型。
4. 上传 `.docx` 或 `.doc`。
5. 等待文档预处理和审查。
6. 左侧问题列表会实时出现。
7. 可以接受、驳回、改写。
8. 右上角下载审查后 Word。

说明：

- 这里上传的是待审 Word，不是上传到 RAG 知识库。
- 当前审查页不支持直接上传 PDF 做多智能体审查。
- 新上传待审 Word 需要 MinerU API。
- 当前 Word 审查不需要 Milvus；但如果同时要用 RAG/知识库页面，Milvus 仍要开。

---

## 五、关停

前端、后端、worker、MinerU API：

```text
Ctrl + C
```

Milvus 如果只是日常开发，可以保持 Docker 容器后台运行。需要停止时：

```powershell
cd D:/work/project/coal-regulation-ai
docker compose -f docker-compose.milvus.yml stop
```

需要彻底停止并移除容器：

```powershell
docker compose -f docker-compose.milvus.yml down
```

`down` 不会删除 `backend/data/milvus_standalone/` 里的数据；不要手动删除该目录。

---

## 六、常见问题

| 现象 | 原因 / 解决 |
| --- | --- |
| Docker Desktop 要登录 | 本地开发不用登录，点 `Skip` |
| `docker` 不是命令 | Docker Desktop 未安装，或安装后没有重启 PowerShell |
| `permission denied ... docker_engine` | 当前终端用户无 Docker engine 权限；用管理员 PowerShell 或确认用户在 `docker-users` 组 |
| Milvus 连接失败 | 先执行 `docker compose -f docker-compose.milvus.yml ps`，确认 standalone 已启动 |
| 上传 PDF/DOCX 卡住 | 多数是 MinerU API 没开，或 MinerU 首次加载较慢 |
| 上传 DOC 卡住 | `.doc` 需要 Word COM 先转 `.docx`，本机需要 Microsoft Word |
| 待审 Word 卡在解析 / 预处理 | 先确认 MinerU API 终端还开着；新上传待审 Word 也会调用 MinerU |
| 知识库上传后 RAG 查不到 | 检查文档状态是否 `indexed`，检查 Milvus 是否运行 |
| worker 一启动就处理历史任务 | 用 `$env:V9_DISABLE_ESCALATION_LOOP="1"` 启动 worker |
| worker 报 `DASHSCOPE_API_KEY` | 检查 `backend/.env` 是否有 `DASHSCOPE_API_KEY=...` |
| CUDA 不可用 | `D:/Anaconda/envs/langchain0.3/python.exe -c "import torch; print(torch.cuda.is_available())"` 应输出 `True` |

---

## 七、命令行版

不开前端，纯脚本审查：

```powershell
cd D:/work/project/coal-regulation-ai
D:/Anaconda/envs/langchain0.3/python.exe hybrid_rag_review_v9.py --max-docs 1
```

主智能体处理升级队列：

```powershell
D:/Anaconda/envs/langchain0.3/python.exe main_agent_v9.py --process-queue
```

更细的架构与接口说明见 [WEB_README_v9.md](WEB_README_v9.md)。
