# 煤矿审查系统 v9 — 运行手册

本系统由**三个进程**组成，需要分别启动（各开一个终端）：

| # | 进程 | 作用 | 运行环境 |
|---|------|------|----------|
| 1 | **v9 worker** | 常驻审查引擎，BGE 模型只加载一次，跑审查 + 主智能体改 Word | langchain0.3 |
| 2 | **后端 FastAPI** | 提供 `/api/v9/*` 接口，前端与 worker 的桥 | langchain0.3 |
| 3 | **前端 React** | 浏览器界面（审查页：问题列表 + PDF 高亮 / 段落视图 + 详情操作） | Node.js |

> 为什么分三个：BGE 本地大模型必须在 langchain0.3 环境且只能加载一次（约 1 分钟），
> 所以独立成常驻 worker；后端不直接加载模型，避免每次请求重载。

---

## 〇、前置检查（第一次运行前做一次）

```powershell
# 1) 确认 langchain0.3 环境存在且依赖齐全
D:/Anaconda/envs/langchain0.3/python.exe -c "import fastapi, FlagEmbedding, torch; print('OK', torch.cuda.is_available())"
# 期望输出: OK True

# 2) 确认本地 BGE 模型在位
Test-Path models/bge-m3
Test-Path models/bge-reranker-v2-m3

# 3) 确认 API key 已配置（worker 会自动从 backend/.env 读取）
#    backend/.env 里应有一行 DASHSCOPE_API_KEY=...

# 4) 确认后端具备 Word 转 PDF 依赖
#    PDF 预览依赖 Windows + Microsoft Word + pywin32
D:/Anaconda/envs/langchain0.3/python.exe -c "import win32com.client; print('Word COM OK')"

# 5) 前端依赖（若没装过）
npm.cmd install
```

> 如果 `npm run dev` 在 PowerShell 中被执行策略拦截，使用 `npm.cmd run dev`。

### 可选：MinerU 文档转 JSON

如果需要把 PDF / Word 先转成本项目既有的 MinerU JSON，再走离线待审切分：

```powershell
# MinerU 已按“同 langchain0.3 环境”接入；国内网络建议用 ModelScope 模型源
$env:MINERU_MODEL_SOURCE="modelscope"
D:/Anaconda/envs/langchain0.3/python.exe -m pip install mineru --isolated -i https://mirrors.aliyun.com/pypi/simple/

# 首次使用前下载 pipeline 本地模型；只需执行一次，之后走本机缓存
D:/Anaconda/envs/langchain0.3/Scripts/mineru-models-download.exe -s modelscope -m pipeline

# 推荐：单独启动常驻 MinerU API，避免每次转换都由 CLI 临时拉起服务
D:/Anaconda/envs/langchain0.3/Scripts/mineru-api.exe --host 127.0.0.1 --port 51071
```

另开一个终端执行转换：

```powershell
# 转待审 PDF / DOCX / DOC 到 new_docs/test_doc_json/
D:/Anaconda/envs/langchain0.3/python.exe mineru_adapter.py "D:/path/to/doc.pdf" --kind pending --api-url http://127.0.0.1:51071

# 转完后顺手生成单文档 chunk 预览到 chunks_visualization/
D:/Anaconda/envs/langchain0.3/python.exe mineru_adapter.py "D:/path/to/doc.pdf" --kind pending --chunk-pending --api-url http://127.0.0.1:51071

# 转规则文档到 new_docs/rule_docs_json/
D:/Anaconda/envs/langchain0.3/python.exe mineru_adapter.py "D:/path/to/rule.pdf" --kind rule --api-url http://127.0.0.1:51071
```

如果不传 `--api-url`，脚本会退回 MinerU CLI 模式；当前测试发现 CLI 自动拉起临时 API 时可能卡在健康检查，所以本项目优先使用上面的常驻 API 方式。

```powershell
D:/Anaconda/envs/langchain0.3/python.exe mineru_adapter.py "D:/path/to/doc.pdf" --kind pending
```

说明：`.doc` 不是 MinerU 原生输入，脚本会先用本机 Word COM 转为 `.docx`；PDF 和 `.docx` 会直接交给 MinerU。
当前环境安装的是基础 `mineru`，不要默认安装 `mineru[all]`；`[all]` 会额外拉 `torch/lmdeploy/ray/gradio` 等重依赖，容易影响 worker 使用的 BGE 环境。
前端「知识库 / RAG」上传 PDF、DOC、DOCX 也会走这条 MinerU API 路径：先生成 `new_docs/rule_docs_json/MinerU_*.json`，再用 `chapter_based_chunking_v6.py` 更新 `chunks_visualization/chunks_v6_latest.json` 并写入当前知识库向量库。若更新了法规库，已经启动的 v9 worker 需要重启一次才会重新加载新的 `chunks_v6_latest.json`。

---

## 一、启动 worker（终端 1）

```powershell
cd D:/work/project/coal-regulation-ai
D:/Anaconda/envs/langchain0.3/python.exe v9_worker.py
```

看到下面这行就绪（首次加载约 1 分钟）：

```
[worker] 引擎就绪，进入轮询循环
```

> 保持这个终端开着。它会一直轮询任务；上传文档后这里会打印审查进度。

如果只是联调前端上传审查，不想让 worker 自动处理历史升级队列，可以先关闭升级队列线程：

```powershell
cd D:/work/project/coal-regulation-ai
$env:V9_DISABLE_ESCALATION_LOOP="1"
D:/Anaconda/envs/langchain0.3/python.exe v9_worker.py
```

这个开关只会关闭历史 `low_confidence` / `numeric_conflict` 等升级项的自动裁决，不影响前端上传 Word 审查，也不影响前端接受 / 驳回 / 改写后落 Word。

---

## 二、启动后端（终端 2）

```powershell
cd D:/work/project/coal-regulation-ai/backend
D:/Anaconda/envs/langchain0.3/python.exe main.py
```

看到：

```
Uvicorn running on http://0.0.0.0:8000
```

> 接口文档可在浏览器打开 http://localhost:8000/api/docs 自检。

---

## 三、启动前端（终端 3）

```powershell
cd D:/work/project/coal-regulation-ai
npm.cmd run dev
```

浏览器会自动打开 http://localhost:3000 ，进入左侧菜单的「**审查**」页。

---

## 四、使用流程

1. 顶部选「非突出矿井 / 突出矿井」，点「**上传待审 Word**」选一个 `.docx` / `.doc`。
2. 系统先进入**文档预处理**：保存原件、统一转换为 `.docx`，并尽量生成 PDF 预览缓存。
3. 预处理成功后才创建审查任务；若 PDF 未生成成功，先进入文本视图，仍可继续审查。
4. 顶部主智能体状态条显示进度；中间面板可在 PDF 高亮预览和文本视图之间切换。
5. 左侧**实时弹出问题**（合规 / 数值 / 错别字 / 重复 / 待裁决）。
6. **点问题** → PDF 或段落视图定位到对应内容，问题原文片段高亮。
7. 每条问题三个操作：
   - **接受**：采纳系统建议，主智能体改写该段落
   - **驳回**：判定系统误报，原文保留
   - **改写**：写下你自己的意见 → 提交，主智能体按你的意见改段落
8. 改写完成后中间面板自动刷新；右上「**下载审查后文档**」导出 Word 工作副本。
9. 如果任务还在运行，可以点「取消任务」停止后续处理。

> 每次接受/驳回/改写都会记入**数据飞轮**（人工层，最高优先级），用于后续扩充评估集。

---

## 五、关停

各终端按 `Ctrl + C`。worker 与后端可随时重启；前端热更新无需重启。

---

## 六、常见问题

| 现象 | 原因 / 解决 |
|------|------------|
| 上传后一直 pending、左侧不出问题 | worker（终端 1）没启动，或还在加载模型；看终端 1 是否打印「引擎就绪」 |
| worker 报 `请设置 DASHSCOPE_API_KEY` | backend/.env 缺 `DASHSCOPE_API_KEY=...`；补上重启 worker |
| worker 报找不到 BGE 模型 | `models/bge-m3`、`models/bge-reranker-v2-m3` 不在位 |
| worker 一启动就处理历史 `low_confidence` / `numeric_conflict` | 这是升级队列自动裁决线程；联调前端时可设置 `$env:V9_DISABLE_ESCALATION_LOOP="1"` 后再启动 worker |
| 上传后卡在预处理 | 多数是 `.doc` 转 `.docx` 或 PDF 预览转换调用 Word COM 较慢；优先上传手动另存后的 `.docx` |
| 中间面板空白 | 等几秒（解析 / 转 PDF 中）；或点文档区「刷新」；或确认上传的是 .docx / .doc |
| PDF 预览加载失败 | 后端运行机器需要 Windows、Microsoft Word 和 `pywin32`；也可以先切到「段落视图」继续看审查结果 |
| 前端 `/api` 404 / 跨域 | 后端没起；确认终端 2 在 :8000 运行（vite 已配代理） |
| CUDA 不可用、跑得很慢 | 前置检查第 1 步 `cuda.is_available()` 应为 True；否则在 CPU 上会慢很多 |
| 切到「突出矿井」结果异常 | 已知限制：索引按非突出矿井构建缓存，突出矿井需单独建索引（见 WEB_README_v9.md） |

---

## 七、命令行版（不开前端，纯脚本审查）

```powershell
# 审查 chunks_visualization 下的待审块（不经 Web）
D:/Anaconda/envs/langchain0.3/python.exe hybrid_rag_review_v9.py --max-docs 1
# 主智能体处理升级队列
D:/Anaconda/envs/langchain0.3/python.exe main_agent_v9.py --process-queue
```

更细的架构与接口说明见 [WEB_README_v9.md](WEB_README_v9.md)。
