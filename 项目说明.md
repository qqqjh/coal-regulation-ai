# 煤炭规程智能体项目说明

## 项目已创建完成

您的煤炭规程智能体前端项目已经全部创建完成。

## 完整的文件结构

```
coal-regulation-ai/
├── public/                          # 静态资源目录
├── src/
│   ├── components/                  # 公共组件目录
│   │   ├── Sidebar/
│   │   │   ├── index.jsx            # 左侧导航栏组件
│   │   │   └── index.css            # 导航栏样式
│   │   │
│   │   ├── AuthRoute/
│   │   │   └── index.jsx            # 路由守卫组件
│   │   │
│   │   ├── MainLayout/
│   │   │   ├── index.jsx            # 主布局组件
│   │   │   └── index.css            # 布局样式
│   │   │
│   │   └── DocumentPreview/
│   │       ├── index.jsx            # 文档预览组件
│   │       └── index.css            # 预览样式
│   │
│   ├── pages/                       # 页面组件目录
│   │   ├── Home/
│   │   │   ├── index.jsx            # 首页（系统介绍）
│   │   │   └── index.css            # 首页样式
│   │   │
│   │   ├── Login/
│   │   │   ├── index.jsx            # 登录页面
│   │   │   └── index.css            # 登录页样式
│   │   │
│   │   ├── Chat/
│   │   │   ├── index.jsx            # 对话页面（核心功能）
│   │   │   └── index.css            # 对话页面样式
│   │   │
│   │   ├── Knowledge/
│   │   │   ├── index.jsx            # 知识库管理页面
│   │   │   └── index.css            # 知识库页面样式
│   │   │
│   │   ├── RAG/
│   │   │   ├── index.jsx            # RAG配置测试页面
│   │   │   └── index.css            # RAG页面样式
│   │   │
│   │   ├── Review/
│   │   │   ├── index.jsx            # 审查修正页面
│   │   │   └── index.css            # 审查页面样式
│   │   │
│   │   └── Monitor/
│   │       ├── index.jsx            # 用量监控页面
│   │       └── index.css            # 监控页面样式
│   │
│   ├── store/
│   │   └── userStore.js             # 用户状态管理（Zustand）
│   │
│   ├── routes/
│   │   └── index.jsx                # 路由配置文件
│   │
│   ├── App.jsx                      # 根组件
│   ├── App.css                      # 根组件样式
│   ├── main.jsx                     # 应用入口文件
│   └── index.css                    # 全局基础样式
│
├── .gitignore                       # Git忽略配置
├── index.html                       # HTML模板
├── package.json                     # 项目依赖配置
├── vite.config.js                   # Vite构建配置
└── 项目说明.md                       # 本文件
```

## 快速开始

### 1. 安装依赖

打开命令行，进入项目目录：

```bash
cd "c:\Users\ZhuanZ（无密码）\Desktop\研究生\前端\coal-regulation-ai"
```

然后安装依赖：

```bash
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

启动后，浏览器会自动打开 http://localhost:3000

### 3. 登录系统

- 演示账号：`admin` / `123456`
- 或输入任意用户名密码登录

## 功能模块详解

### 0. 首页 (/)

**主要功能：**
- 系统介绍和欢迎信息
- 核心功能卡片导航
- 多智能体协同架构展示
- 用户个性化欢迎

**使用场景：**
用户登录后首先看到系统介绍，了解各功能模块，点击卡片进入对应功能。

---

### 1. 对话模块 (/chat)

**主要功能：**
- 多会话管理（新建、切换、删除、重命名）
- 实时消息发送与接收
- Markdown 格式渲染
- 历史对话记录
- 模拟 AI 智能回复
- 滚动自动定位到最新消息
- 会话重命名（点击编辑图标）

**使用场景：**
用户提问煤炭规程相关问题，AI 基于知识库给出专业解答。

---

### 2. 知识库管理模块 (/knowledge)

**主要功能：**
- 知识库 CRUD 操作
- 文档上传（PDF、Word、TXT）
- 文档列表查看
- 文档预览功能（内容和信息两个标签页）
- 索引状态管理
- 统计数据展示（知识库数、文档数、存储空间）

**使用场景：**
管理员上传煤矿安全规程、技术标准等文档，系统自动索引用于后续检索。

---

### 3. RAG 配置模块 (/rag)

**主要功能：**
- 检索参数配置（检索方式、Top-K、相似度阈值）
- 生成参数配置（模型、温度、最大长度）
- 实时检索测试
- 检索结果展示（文档片段、相似度评分）
- 答案生成与预览
- 引用来源标注

**使用场景：**
技术人员调整 RAG 参数，测试不同配置下的检索效果和答案质量。

---

### 4. 审查修正模块 (/review)

**主要功能：**
- 多智能体协同处理流程可视化
  - 全域知识检索智能体
  - 合规性判别智能体
  - 全局依赖分析智能体
  - 标准内容生成智能体
  - 安全合规复合智能体
- 文档上传（PDF、Word、TXT）
- 处理进度实时展示
- 问题诊断报告（严重/警告/建议分类）
- 知识库参考展示
- 依赖关系分析
- 修正报告导出

**使用场景：**
上传待审查文档，系统通过多智能体协同完成合规性检查，生成修正建议。

---

### 5. 用量监控模块 (/monitor)

**主要功能：**
- 实时统计（调用次数、Token使用、成本、响应时间）
- 调用趋势图（折线图）
- 功能使用分布（饼图）
- Token消耗统计（柱状图）
- 详细调用日志
- 时间范围和模块筛选
- 数据导出功能

**使用场景：**
管理员监控系统使用情况，分析成本和性能，优化资源配置。

## 用户认证系统

### 登录功能
- 用户名密码登录
- 登录状态持久化（localStorage）
- 路由守卫（未登录自动跳转登录页）

### 用户信息展示
- 侧边栏底部显示用户头像和名称
- 下拉菜单支持：个人信息、设置、退出登录

## UI 设计亮点

1. **现代化设计**：采用 Ant Design 组件库，界面简洁美观
2. **深色侧边栏**：专业感强，视觉层次清晰
3. **响应式布局**：适配不同屏幕尺寸
4. **流畅动画**：消息淡入、按钮悬停、智能体处理进度等交互效果
5. **数据可视化**：使用 Recharts 图表库，数据展示直观
6. **登录页动画背景**：渐变色背景配合浮动形状动画

## 技术实现特点

### 1. 无后端依赖运行
- 所有功能使用 Mock 数据
- 模拟 API 延迟效果
- 完整的前端交互逻辑

### 2. 状态管理
- 使用 Zustand 管理用户状态
- 状态持久化到 localStorage
- 登录状态自动恢复

### 3. 模块化设计
- 组件独立，职责单一
- 样式文件分离
- 路由配置清晰

### 4. 用户体验优化
- Loading 状态提示
- 操作成功/失败反馈
- 表单验证
- 二次确认（删除操作）

## Mock 数据说明

### 对话模块
- 预设 3 种不同风格的 AI 回复
- 随机选择回复内容
- 1秒延迟模拟真实场景

### 知识库模块
- 预置 2 个知识库
- 预置 3 个文档
- 上传文档自动生成 Mock 数据

### RAG 模块
- 预设 5 条检索结果
- 根据相似度阈值过滤
- 生成综合性答案

### 审查修正模块
- 模拟 5 个智能体处理流程
- 预设 8 个问题（2严重、4警告、2建议）
- 知识库参考和依赖关系数据

### 监控模块
- 模拟 24 小时调用趋势
- 4 个功能模块的使用分布
- 8 条调用日志记录

## 后端接口对接指南

当您有后端 API 时，需要进行以下修改：

### 1. 创建 API 服务层

创建 `src/services/api.js`：

```javascript
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://your-api-server.com/api',
  timeout: 10000
})

// 请求拦截器 - 添加 token
api.interceptors.request.use(config => {
  const userStorage = localStorage.getItem('user-storage')
  if (userStorage) {
    const { state } = JSON.parse(userStorage)
    if (state.user?.token) {
      config.headers.Authorization = `Bearer ${state.user.token}`
    }
  }
  return config
})

export const authAPI = {
  login: (data) => api.post('/auth/login', data),
  logout: () => api.post('/auth/logout')
}

export const chatAPI = {
  sendMessage: (data) => api.post('/chat/send', data),
  getHistory: (sessionId) => api.get(`/chat/history/${sessionId}`)
}

export const knowledgeAPI = {
  getList: () => api.get('/knowledge/list'),
  create: (data) => api.post('/knowledge/create', data),
  uploadDoc: (kbId, file) => {
    const formData = new FormData()
    formData.append('file', file)
    return api.post(`/knowledge/${kbId}/upload`, formData)
  }
}

export const ragAPI = {
  retrieve: (data) => api.post('/rag/retrieve', data),
  generate: (data) => api.post('/rag/generate', data)
}

export const reviewAPI = {
  upload: (file) => {
    const formData = new FormData()
    formData.append('file', file)
    return api.post('/review/upload', formData)
  },
  getResult: (taskId) => api.get(`/review/result/${taskId}`)
}

export const monitorAPI = {
  getStats: (params) => api.get('/monitor/stats', { params }),
  getLogs: (params) => api.get('/monitor/logs', { params })
}
```

### 2. 替换组件中的 Mock 逻辑

以对话模块为例：

```javascript
// 原来的 Mock 代码
setTimeout(() => {
  const aiMessage = { ... }
  // ...
}, 1000)

// 改为真实 API 调用
try {
  const response = await chatAPI.sendMessage({
    sessionId: currentSessionId,
    content: inputValue
  })
  const aiMessage = {
    id: response.data.id,
    role: 'assistant',
    content: response.data.content,
    timestamp: response.data.timestamp
  }
  // 更新状态
} catch (error) {
  message.error('发送失败：' + error.message)
}
```

## 已实现功能清单

- [x] 用户登录认证
- [x] 登录状态持久化
- [x] 路由守卫
- [x] 首页系统介绍
- [x] 智能对话（多会话管理）
- [x] 会话重命名
- [x] 知识库管理
- [x] 文档预览功能
- [x] RAG 配置测试
- [x] 审查修正（多智能体协同）
- [x] 用量监控
- [x] 用户信息展示和退出登录

## 下一步建议

### 功能增强
1. 实现对话导出功能
2. 支持多文件批量上传
3. 添加深色模式切换
4. 实现 WebSocket 实时通信
5. 添加消息流式输出效果

### 性能优化
1. 使用 React.memo 优化组件渲染
2. 实现虚拟滚动（长列表）
3. 添加图片懒加载
4. 代码分割和按需加载

## 常见问题

### Q: 为什么运行后看不到页面？
A: 检查是否正确安装了依赖，特别是 `antd` 和 `react-router-dom`

### Q: 如何修改默认端口？
A: 修改 `vite.config.js` 中的 `server.port` 配置

### Q: 图标没有显示？
A: 确保 `@ant-design/icons` 已正确安装

### Q: 如何添加新菜单？
A:
1. 在 `src/pages/` 创建新页面
2. 在 `src/routes/index.jsx` 添加路由
3. 在 `src/components/Sidebar/index.jsx` 添加菜单项

### Q: PowerShell 无法运行 npm？
A: 可以使用以下方案之一：
1. 以管理员身份运行 `Set-ExecutionPolicy RemoteSigned`
2. 使用 CMD 命令行代替 PowerShell
3. 临时绕过：`powershell -ExecutionPolicy Bypass -Command "npm run dev"`

## 技术支持

如遇到问题，请检查：
1. Node.js 版本 >= 16
2. npm 版本 >= 8
3. 依赖是否完整安装
4. 浏览器控制台错误信息

---

## 总结

您现在拥有一个功能完整的煤炭规程智能体前端系统：

- 6 个核心功能模块（首页、对话、知识库、RAG、审查修正、监控）
- 完整的用户认证系统
- 多智能体协同审查功能
- 完整的 UI 交互
- Mock 数据支持
- 无需后端即可运行
- 清晰的代码结构

**立即开始体验吧！**

```bash
npm install && npm run dev
```

祝您开发顺利！
