# 煤炭规程智能体 - 技术文档

## 1. 项目概述

煤炭规程智能体是一个基于大语言模型的煤炭行业规程智能助手前端系统，提供规程检索、智能问答、文档审查等一站式服务。

### 1.1 核心功能

| 功能模块 | 描述 |
|---------|------|
| 首页 | 系统介绍、功能导航 |
| 智能对话 | 基于知识库的智能问答 |
| 知识库管理 | 文档上传、管理、检索 |
| RAG配置 | 检索增强生成参数配置 |
| 审查修正 | 多智能体协同文档审查 |
| 用量监控 | 系统使用情况监控 |

### 1.2 技术架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端应用层                            │
├─────────────────────────────────────────────────────────────┤
│  React 18  │  React Router v6  │  Zustand  │  Ant Design 5 │
├─────────────────────────────────────────────────────────────┤
│                        构建工具层                            │
├─────────────────────────────────────────────────────────────┤
│              Vite 5  │  ESBuild  │  Rollup                  │
├─────────────────────────────────────────────────────────────┤
│                        基础设施层                            │
├─────────────────────────────────────────────────────────────┤
│           Node.js  │  npm  │  JavaScript/JSX                │
└─────────────────────────────────────────────────────────────┘
```

## 2. 技术栈详解

### 2.1 核心框架

#### React 18
- **版本**: ^18.2.0
- **作用**: 前端 UI 框架
- **特性使用**:
  - 函数式组件 + Hooks
  - useState, useEffect, useRef 状态管理
  - 自动批处理更新
  - Concurrent Mode 并发渲染

```javascript
// 示例：使用 React Hooks
const Chat = () => {
  const [messages, setMessages] = useState([])
  const [isLoading, setIsLoading] = useState(false)
  const messagesEndRef = useRef(null)

  useEffect(() => {
    scrollToBottom()
  }, [messages])

  // ...
}
```

#### React Router v6
- **版本**: ^6.21.0
- **作用**: 前端路由管理
- **特性使用**:
  - 声明式路由配置
  - 嵌套路由
  - 路由守卫
  - useNavigate, useLocation Hooks

```javascript
// 路由配置示例
<Routes>
  <Route path="/login" element={<Login />} />
  <Route path="/*" element={
    <AuthRoute>
      <MainLayout />
    </AuthRoute>
  }>
    <Route index element={<Home />} />
    <Route path="chat" element={<Chat />} />
    <Route path="knowledge" element={<Knowledge />} />
  </Route>
</Routes>
```

### 2.2 UI 组件库

#### Ant Design 5
- **版本**: ^5.12.0
- **作用**: 企业级 UI 组件库
- **使用组件**:

| 组件类型 | 组件名称 | 使用场景 |
|---------|---------|---------|
| 布局 | Layout, Sider, Menu | 页面整体布局 |
| 数据录入 | Input, Select, Form, Upload | 表单输入 |
| 数据展示 | Card, Table, List, Tag, Statistic | 数据展示 |
| 反馈 | Modal, Message, Progress, Spin | 用户反馈 |
| 导航 | Dropdown, Steps | 导航交互 |

```javascript
// Ant Design 组件使用示例
import { Card, Button, Table, message } from 'antd'
import { SearchOutlined, UploadOutlined } from '@ant-design/icons'

const Knowledge = () => {
  return (
    <Card title="知识库管理" extra={
      <Button type="primary" icon={<UploadOutlined />}>
        上传文档
      </Button>
    }>
      <Table columns={columns} dataSource={data} />
    </Card>
  )
}
```

#### @ant-design/icons
- **版本**: ^5.2.6
- **作用**: Ant Design 官方图标库
- **常用图标**:
  - MessageOutlined - 对话
  - DatabaseOutlined - 知识库
  - SearchOutlined - 检索
  - SafetyCertificateOutlined - 审查
  - DashboardOutlined - 监控
  - HomeOutlined - 首页

### 2.3 状态管理

#### Zustand
- **版本**: ^4.4.7
- **作用**: 轻量级状态管理库
- **特性**:
  - 极简 API
  - 无 Provider 包装
  - 支持中间件（persist 持久化）
  - TypeScript 友好

```javascript
// store/userStore.js
import { create } from 'zustand'
import { persist } from 'zustand/middleware'

const useUserStore = create(
  persist(
    (set) => ({
      user: null,
      isLoggedIn: false,
      login: (userInfo) => set({
        user: userInfo,
        isLoggedIn: true
      }),
      logout: () => set({
        user: null,
        isLoggedIn: false
      }),
      updateUser: (userInfo) => set((state) => ({
        user: { ...state.user, ...userInfo }
      }))
    }),
    {
      name: 'user-storage' // localStorage key
    }
  )
)

export default useUserStore
```

**使用方式**:
```javascript
// 在组件中使用
const user = useUserStore((state) => state.user)
const login = useUserStore((state) => state.login)
const logout = useUserStore((state) => state.logout)
```

### 2.4 数据可视化

#### Recharts
- **版本**: ^2.10.3
- **作用**: 基于 React 的图表库
- **使用图表**:

| 图表类型 | 组件 | 使用场景 |
|---------|------|---------|
| 折线图 | LineChart | 调用趋势 |
| 柱状图 | BarChart | Token消耗统计 |
| 饼图 | PieChart | 功能使用分布 |

```javascript
// 图表使用示例
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from 'recharts'

<ResponsiveContainer width="100%" height={300}>
  <LineChart data={trendData}>
    <CartesianGrid strokeDasharray="3 3" />
    <XAxis dataKey="time" />
    <YAxis />
    <Tooltip />
    <Legend />
    <Line type="monotone" dataKey="calls" stroke="#1890ff" name="调用次数" />
    <Line type="monotone" dataKey="tokens" stroke="#52c41a" name="Token消耗" />
  </LineChart>
</ResponsiveContainer>
```

### 2.5 Markdown 渲染

#### react-markdown
- **版本**: ^9.0.1
- **作用**: Markdown 内容渲染
- **使用场景**: AI 回复内容展示

```javascript
import ReactMarkdown from 'react-markdown'

// 渲染 AI 回复
<div className="message-content">
  {msg.role === 'assistant' ? (
    <ReactMarkdown>{msg.content}</ReactMarkdown>
  ) : (
    msg.content
  )}
</div>
```

### 2.6 日期处理

#### dayjs
- **版本**: ^1.11.10
- **作用**: 轻量级日期处理库
- **使用场景**: 时间格式化、日期选择器

```javascript
import dayjs from 'dayjs'

// 日期格式化
const formattedDate = dayjs().format('YYYY-MM-DD HH:mm:ss')

// 日期计算
const weekAgo = dayjs().subtract(7, 'day')
```

### 2.7 构建工具

#### Vite 5
- **版本**: ^5.0.0
- **作用**: 下一代前端构建工具
- **优势**:
  - 极速冷启动
  - 即时热更新 (HMR)
  - 按需编译
  - 原生 ESM 支持

**配置文件 (vite.config.js)**:
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    open: true
  },
  build: {
    outDir: 'dist',
    sourcemap: false
  }
})
```

## 3. 项目结构

```
coal-regulation-ai/
├── public/                     # 静态资源
├── src/
│   ├── components/             # 公共组件
│   │   ├── Sidebar/            # 侧边栏导航
│   │   ├── AuthRoute/          # 路由守卫
│   │   ├── MainLayout/         # 主布局
│   │   └── DocumentPreview/    # 文档预览
│   │
│   ├── pages/                  # 页面组件
│   │   ├── Home/               # 首页
│   │   ├── Login/              # 登录页
│   │   ├── Chat/               # 对话页
│   │   ├── Knowledge/          # 知识库页
│   │   ├── RAG/                # RAG配置页
│   │   ├── Review/             # 审查修正页
│   │   └── Monitor/            # 用量监控页
│   │
│   ├── store/                  # 状态管理
│   │   └── userStore.js        # 用户状态
│   │
│   ├── routes/                 # 路由配置
│   │   └── index.jsx           # 路由定义
│   │
│   ├── App.jsx                 # 根组件
│   ├── App.css                 # 根样式
│   ├── main.jsx                # 入口文件
│   └── index.css               # 全局样式
│
├── index.html                  # HTML 模板
├── package.json                # 依赖配置
├── vite.config.js              # Vite 配置
└── .gitignore                  # Git 忽略
```

## 4. 核心模块设计

### 4.1 用户认证模块

**流程图**:
```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  登录页  │───>│  验证    │───>│  存储    │───>│  跳转    │
│          │    │  用户名  │    │  状态到  │    │  首页    │
│          │    │  密码    │    │localStorage│   │          │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

**路由守卫实现**:
```javascript
// components/AuthRoute/index.jsx
import { Navigate } from 'react-router-dom'
import useUserStore from '../../store/userStore'

const AuthRoute = ({ children }) => {
  const isLoggedIn = useUserStore((state) => state.isLoggedIn)

  if (!isLoggedIn) {
    return <Navigate to="/login" replace />
  }

  return children
}
```

### 4.2 多智能体协同模块

**审查修正流程**:
```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│ 全域知识检索 │──>│ 合规性判别  │──>│ 全局依赖分析│
│   智能体    │   │   智能体    │   │   智能体    │
└─────────────┘   └─────────────┘   └─────────────┘
                                           │
                                           ▼
                  ┌─────────────┐   ┌─────────────┐
                  │ 安全合规复合 │<──│ 标准内容生成│
                  │   智能体    │   │   智能体    │
                  └─────────────┘   └─────────────┘
```

**智能体配置**:
```javascript
const agents = [
  {
    key: 'search',
    name: '全域知识检索智能体',
    description: '知识库检索（国标/行业/企业）',
    output: '跨文档背景知识召回',
    color: '#1890ff'
  },
  {
    key: 'compliance',
    name: '合规性判别智能体',
    description: '识别参数/程序违规',
    output: '错误诊断报告',
    color: '#52c41a'
  },
  // ... 其他智能体
]
```

### 4.3 对话管理模块

**数据结构**:
```javascript
// 会话结构
const session = {
  id: number,           // 会话ID
  name: string,         // 会话名称
  createdAt: string,    // 创建时间 ISO格式
  messages: Message[]   // 消息列表
}

// 消息结构
const message = {
  id: number,           // 消息ID
  role: 'user' | 'assistant',  // 角色
  content: string,      // 内容（支持Markdown）
  timestamp: string     // 时间戳
}
```

**状态管理**:
```javascript
const [sessions, setSessions] = useState([])        // 会话列表
const [currentSessionId, setCurrentSessionId] = useState(null)  // 当前会话
const [inputValue, setInputValue] = useState('')    // 输入内容
const [isGenerating, setIsGenerating] = useState(false)  // 生成状态
const [editingSessionId, setEditingSessionId] = useState(null)  // 编辑会话
```

## 5. 样式设计

### 5.1 设计规范

**颜色体系**:
| 用途 | 颜色值 | 说明 |
|-----|-------|------|
| 主色 | #1890ff | Ant Design 蓝 |
| 成功 | #52c41a | 绿色 |
| 警告 | #faad14 | 橙色 |
| 错误 | #ff4d4f | 红色 |
| 紫色 | #722ed1 | 审查修正 |
| 青色 | #13c2c2 | 辅助色 |

**间距规范**:
- 小间距: 8px
- 中间距: 16px
- 大间距: 24px
- 超大间距: 48px

### 5.2 布局设计

```css
/* 整体布局 */
.app-container {
  display: flex;
  min-height: 100vh;
}

.sidebar {
  width: 220px;
  position: fixed;
  left: 0;
  top: 0;
  bottom: 0;
}

.main-content {
  flex: 1;
  margin-left: 220px;
  padding: 24px;
}
```

### 5.3 响应式设计

```css
/* 断点设计 */
@media (max-width: 1200px) {
  /* 大屏幕适配 */
}

@media (max-width: 992px) {
  /* 中等屏幕适配 */
}

@media (max-width: 768px) {
  /* 平板适配 */
}

@media (max-width: 576px) {
  /* 手机适配 */
}
```

## 6. 开发规范

### 6.1 代码规范

**组件结构**:
```javascript
// 推荐的组件结构
import { useState, useEffect } from 'react'
import { Component } from 'antd'
import './index.css'

const MyComponent = () => {
  // 1. Hooks 声明
  const [state, setState] = useState()

  // 2. 副作用
  useEffect(() => {
    // ...
  }, [])

  // 3. 事件处理函数
  const handleClick = () => {
    // ...
  }

  // 4. 渲染
  return (
    <div className="my-component">
      {/* JSX */}
    </div>
  )
}

export default MyComponent
```

### 6.2 命名规范

| 类型 | 规范 | 示例 |
|-----|------|------|
| 组件 | PascalCase | `ChatMessage` |
| 文件夹 | PascalCase | `ChatMessage/` |
| 函数 | camelCase | `handleSubmit` |
| 常量 | UPPER_SNAKE_CASE | `MAX_LENGTH` |
| CSS类 | kebab-case | `.chat-message` |
| 事件处理 | handle + 动作 | `handleClick` |

### 6.3 文件组织

```
ComponentName/
├── index.jsx       # 组件主文件
├── index.css       # 组件样式
├── utils.js        # 工具函数（可选）
└── constants.js    # 常量定义（可选）
```

## 7. 性能优化

### 7.1 已实施优化

1. **组件懒加载**: 路由级别代码分割
2. **状态持久化**: localStorage 缓存用户状态
3. **按需引入**: Ant Design 组件按需加载
4. **图片优化**: SVG 图标使用

### 7.2 优化建议

```javascript
// 1. 使用 React.memo 优化渲染
const MessageItem = React.memo(({ message }) => {
  return <div>{message.content}</div>
})

// 2. 使用 useMemo 缓存计算
const filteredData = useMemo(() => {
  return data.filter(item => item.status === filter)
}, [data, filter])

// 3. 使用 useCallback 缓存函数
const handleClick = useCallback(() => {
  // ...
}, [dependencies])
```

## 8. 安全考虑

### 8.1 前端安全措施

1. **XSS 防护**: React 默认转义，Markdown 使用安全库
2. **路由守卫**: 未登录用户无法访问受保护页面
3. **状态隔离**: 用户数据存储在 localStorage

### 8.2 后端对接安全建议

```javascript
// API 请求拦截器
api.interceptors.request.use(config => {
  const token = getToken()
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

// 响应拦截器
api.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      logout()
      redirect('/login')
    }
    return Promise.reject(error)
  }
)
```

## 9. 部署指南

### 9.1 开发环境

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 访问 http://localhost:3000
```

### 9.2 生产构建

```bash
# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 9.3 部署配置

**Nginx 配置示例**:
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend-server;
    }
}
```

## 10. 依赖版本清单

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.21.0",
    "antd": "^5.12.0",
    "@ant-design/icons": "^5.2.6",
    "zustand": "^4.4.7",
    "recharts": "^2.10.3",
    "react-markdown": "^9.0.1",
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
```

## 11. 更新日志

### v1.0.0 (2024-01)
- 初始版本发布
- 实现用户登录认证
- 实现智能对话功能
- 实现知识库管理
- 实现 RAG 配置
- 实现审查修正（多智能体协同）
- 实现用量监控

---

**文档版本**: 1.0.0
**最后更新**: 2024年1月
**维护者**: 煤炭规程智能体开发团队
